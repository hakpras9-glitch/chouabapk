name: Android CI

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Build Force APK
      run: |
        export org.gradle.jvmargs="-Xmx2g"
        if [ -f "gradlew" ]; then
          chmod +x gradlew
          ./gradlew assembleDebug --continue || ./gradlew assemble --continue || true
        else
          gradle assembleDebug --continue || true
        fi

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: app-debug
        path: |
          **/build/outputs/apk/**/*.apk
          *.apk
        if-no-files-found: ignore
